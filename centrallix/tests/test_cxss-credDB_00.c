#include <assert.h>
#include <stdio.h>
#include "cxss/credentials_db.h"

long long
test(char** name)
    {
     *name = "CXSS Cred DB 00: Basic Init";

    /** Basic test of init and release */
    

    return 0;
    }
